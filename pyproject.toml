[project]
name = "cp-app"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.4",
    "psycopg2-binary>=2.9.10",
    "python-dotenv>=1.1.1",
]

dev-dependencies = [
    "pytest>=7.4.0",
    "pytest-django>=4.5.2",
    "black>=25.1.0",
]
